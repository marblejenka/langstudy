(ns test)

(def current-track (atom "initial state of current track"))
(def current-composer (atom "initial state of current composer"))

(def music-player (atom {:title "initial title" :composer "initial composer"}))

